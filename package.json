{
  "name": "menger",
  "version": "1.0.0",
  "description": "Tool to play around with a menger sponge.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "elm-make src/*.elm --warn --output=build/js/main.js",
    "live": "elm-live src/*.elm --warn --output=build/js/main.js",
    "clean": "rm build/js/main.js"
  },
  "devDependencies": {
    "electron": "^1.8.4",
    "elm": "0.18.0"
  }
}
